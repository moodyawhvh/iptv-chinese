# 工作流名称:update(每日更新)
# 作用:每天 UTC 0:00 定时处理已批准的 issue 请求,更新内部播放列表并重新生成公开播放列表,
#      最后把结果提交回仓库并部署到 GitHub Pages。对应文档:docs/workflows.md#update。
name: update
on:
  workflow_dispatch:        # 允许手动触发(便于调试与补跑)
  schedule:
    - cron: '0 0 * * *'     # 每天 UTC 0:00 定时执行
permissions:
  contents: read            # 推送等写操作通过下面的 GitHub App token 完成
jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      # 用 GitHub App (iptv-bot) 换取临时 token,保证机器人提交有合法身份
      - uses: tibdex/github-app-token@v1.8.2
        if: ${{ !env.ACT }}   # env.ACT:用 nektos/act 在本地调试时跳过需要 secrets 的步骤
        id: create-app-token
        with:
          app_id: ${{ secrets.APP_ID }}
          private_key: ${{ secrets.APP_PRIVATE_KEY }}
      - uses: actions/checkout@v6
        if: ${{ !env.ACT }}
        with:
          token: ${{ steps.create-app-token.outputs.token }}
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: 'npm'
      - name: Install dependencies # 安装 npm 依赖
        run: npm install
      # 1) 处理 issue 中已批准的增删改请求,更新内部播放列表(streams/)
      - name: Update internal playlists
        id: playlist-update
        run: |
          npm run playlist:update
          echo "processed_issues=$(cat temp/logs/playlist_update.log)" >> $GITHUB_OUTPUT
      # 2) 对更新后的内部播放列表做语法检查与数据校验
      - name: Check internal playlists
        run: |
          npm run playlist:lint
          npm run playlist:validate
      # 3) 生成面向普通用户的公开播放列表
      - name: Generate public playlists
        run: npm run playlist:generate
      # 4) 导出全部直播流数据,供 iptv-org/api 使用
      - name: Create .api/streams.json
        run: npm run playlist:export
      # 5) 刷新 PLAYLISTS.md 中的播放列表清单
      - name: Update readme
        run: npm run readme:update
      - name: Setup git # 为后续提交配置机器人身份
        run: |
          git config user.name "iptv-bot[bot]"
          git config user.email "84861620+iptv-bot[bot]@users.noreply.github.com"
      # 有实际处理的 issue 时才提交 /streams 的变更(允许空提交以保持流程统一)
      - name: Commit changes to /streams
        if: steps.playlist-update.outputs.processed_issues != 0
        run: |
          git add streams
          git status
          git commit --allow-empty -m "[Bot] Update /streams" -m "Committed by [iptv-bot](https://github.com/apps/iptv-bot) via [update](https://github.com/iptv-org/iptv/actions/runs/${{ github.run_id }}) workflow." -m "${{ steps.playlist-update.outputs.processed_issues }}" --no-verify
      - name: Commit changes to PLAYLIST.md # 提交 PLAYLISTS.md 清单更新
        run: |
          git add PLAYLISTS.md
          git status
          git commit --allow-empty -m "[Bot] Update PLAYLISTS.md" -m "Committed by [iptv-bot](https://github.com/apps/iptv-bot) via [update](https://github.com/iptv-org/iptv/actions/runs/${{ github.run_id }}) workflow." --no-verify
      # 仅在 master 分支且非本地 act 调试时才真正推送
      - name: Push all changes to the repository
        if: ${{ !env.ACT && github.ref == 'refs/heads/master' }}
        run: git push
      # 6) 把公开播放列表部署到 iptv-org/iptv 的 gh-pages 分支(GitHub Pages)
      - name: Deploy public playlists to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@4.1.1
        if: ${{ !env.ACT && github.ref == 'refs/heads/master' }}
        with:
          repository-name: iptv-org/iptv
          branch: gh-pages
          folder: .gh-pages
          token: ${{ steps.create-app-token.outputs.token }}
          git-config-name: iptv-bot[bot]
          git-config-email: 84861620+iptv-bot[bot]@users.noreply.github.com
          commit-message: '[Bot] Deploy to GitHub Pages'
          clean: true
      # 7) 把 .api/streams.json 部署到 iptv-org/api 的 gh-pages 分支(clean: false 保留该分支其他文件)
      - name: Move .api/streams.json to iptv-org/api
        uses: JamesIves/github-pages-deploy-action@4.1.1
        if: ${{ !env.ACT && github.ref == 'refs/heads/master' }}
        with:
          repository-name: iptv-org/api
          branch: gh-pages
          folder: .api
          token: ${{ steps.create-app-token.outputs.token }}
          git-config-name: iptv-bot[bot]
          git-config-email: 84861620+iptv-bot[bot]@users.noreply.github.com
          commit-message: '[Bot] Deploy to iptv-org/api'
          clean: false
